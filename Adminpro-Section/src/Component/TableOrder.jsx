import { useState } from 'react';
import { Link } from 'react-router-dom';

const TableOrder = ({ title }) => {
  // State for table data - initially empty
  const [tableData] = useState({ data: [] });

  
};

export default TableOrder;